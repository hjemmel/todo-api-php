name: delete_branch

on: [pull_request]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: jessfraz/branch-cleanup-action@master
      with:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
