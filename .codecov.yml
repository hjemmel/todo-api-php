codecov:
  require_ci_to_pass: true
  archive:
    uploads: true

coverage:
  status:
    project:
      default:
        target: 70%
        threshold: null
  precision: 2
  round: down
  range: "70...100"

comment:
  layout: "header, diff, changes, sunburst, uncovered"